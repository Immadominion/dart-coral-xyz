name: coral_xyz
description: |
  A Dart client for Anchor programs on Solana.
  Provides type-safe program interactions, IDL parsing, and seamless integration
  with Solana's Anchor framework.
version: 1.0.0-beta.8
homepage: https://github.com/Immadominion/dart-coral-xyz
repository: https://github.com/Immadominion/dart-coral-xyz
issue_tracker: https://github.com/Immadominion/dart-coral-xyz/issues
documentation: https://pub.dev/documentation/coral_xyz

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # Core Dart dependencies
  base_codecs: ^1.0.1                  # Additional base encodings
  blockchain_utils: ^5.2.0             # Enhanced blockchain utilities (updated)
  bs58: ^1.0.2                         # Base58 encoding (Bitcoin-style)
  convert: ^3.1.1
  crypto: ^3.0.3                       # SHA256 and other hash functions
  cryptography: ^2.7.0                 # ED25519, X25519, and other crypto
  ed25519_hd_key: ^2.3.0               # HD key derivation for wallet support
  equatable: ^2.0.5                    # Value equality
  http: ^1.2.0                         # HTTP client (updated)
  logging: ^1.3.0                      # Proper logging framework
  meta: ^1.16.0                        # Dart meta annotations (updated)
  path: ^1.9.0                         # Path manipulation utilities (updated)
  solana: ^0.31.2+1                    # Primary Solana RPC client
  web_socket_channel: '>=2.0.0 <4.0.0' # WebSocket support (relaxed for compatibility)
  
  # Codegen dependencies - MUST be in dependencies (not dev_dependencies)
  # because they're imported in lib/src/codegen/*.dart files
  analyzer: ^8.4.0                     # Required for codegen in lib/
  build: ^4.0.2                        # Required for codegen in lib/
  source_gen: ^4.0.2                   # Required for codegen in lib/

dev_dependencies:
  build_runner: ^2.9.0                 # Build system runner (updated)
  lints: ^6.0.0
  mockito: ^5.4.4                      # Mocking framework (updated)
  test: ^1.25.0                        # Test framework (updated)

# Build configuration for code generation
build_runner:
  builders:
    coral_xyz:
      import: "package:coral_xyz/builder.dart"
      builder_factories:
        - anchorBuilder
      build_extensions:
        ".dart": [".anchor.dart"]
      auto_apply: dependents
      build_to: source
      applies_builders: ["source_gen:combining_builder"]

# Uncomment for Flutter package
# flutter:
#   plugin:
#     platforms:
#       android:
#         package: com.example.coral_xyz_anchor
#         pluginClass: CoralXyzAnchorPlugin
#       ios:
#         pluginClass: CoralXyzAnchorPlugin

topics:
  - solana
  - anchor
  - blockchain
  - web3
  - crypto

# screenshots:
#   - description: 'Basic usage example'
#     path: screenshots/example.png
